# ANVIL — 第一性原理（First Principles）

> 宪法。所有决策从不可再分的真理出发向上推导。
> 不参考任何其他工具"怎么做"。

---

## 一、不可再分的基本真理

### 真理 1：编程的本质

编程 = 人类对计算机下达精确指令的过程。

这个过程需要的核心能力：
- **阅读**现有代码（理解当前状态）
- **编写**新代码（描述目标状态）
- **执行**代码（验证正确性）
- **搜索**信息（查找引用、文档、方案）

### 真理 2：AI 的现状

当前最好的 AI 模型：
- 能理解自然语言描述的目标
- 能生成代码片段
- 有幻觉，需要验证
- 训练数据剪枝后，不具备实时信息

因此 AI 编程助手需要：
- 提供给 AI 的项目上下文（文件、git、目录结构）
- 让 AI 能执行操作的接口（读/写/搜索/运行）
- 验证机制（执行代码 + 反馈）

### 真理 3：供应商中性

AI 模型本身不是编程工具的核心价值。
核心价值是**人机协作的流程和工具链**。
模型只是引擎，可以随时更换。

因此：
- 协议层必须抽象（OpenAI 兼容协议是事实标准）
- API key、端点、模型名必须可配置
- 不能有硬编码的供应商依赖

### 真理 4：用户主权

用户应该完全拥有：
- 自己的代码（当然）
- 自己的配置（全部在一个目录下，可迁移）
- 自己的对比数据（知道工具在变好还是变差）
- 选择模型的权利（不被锁定）

---

## 二、从真理到架构

### 阅读 → `read_file` + `grep_search` + `glob_search`
不需要更复杂的东西。不需要 LSP 索引、不需要 AST 解析器。
最直接的方式：读文件内容 + 正则搜索 + 文件名匹配。

### 编写 → `write_file` + `edit_file` (patch)
创建新文件用写入，修改现有文件用 patch。
不引入 diff 格式争论，不引入 merge 引擎。
先写入，再验证，出错了重来。

### 执行 → `bash`
Shell 是编程领域最通用的执行环境。
不需要 Docker（默认）、不需要沙箱（可选）。
一个 bash 工具覆盖：编译、测试、运行、安装、部署。

### 搜索 → `WebSearch` + `WebFetch`
搜索引擎是互联网的入口。
不绑定任何搜索引擎（`ANVIL_WEB_SEARCH_BASE_URL` 可配）。
不引入向量数据库、RAG 等复杂方案（除非数据证明需要）。

### 配置 → `~/.anvil/settings.json`
所有配置一个文件。JSON，人类可读可写。
不依赖环境变量（等注入），不依赖 shell profile。
API key、模型别名、搜索地址、权限，全在这里。

### 对比 → `anvil compare`
进步需要度量。没有度量就没有优化方向。
不和任何特定工具对比——和"最好的可用方案"对比。
如果哪天出现了比 Claude Code 更好的工具，anvil 就和它比。

---

## 三、决策树

```
这个改动解决哪个基本真理？
  ↓
解决方式是不是最直接的？
  ├─ 是 → 做
  └─ 否 →
      更直接的方式是什么？
      ↓
      它有没有引入新的复杂性？
      ├─ 没有 → 换用更直接的方式
      └─ 有 →
          权衡：增加的复杂性值不值得？
          ├─ 值得 → 做，但保留简化路径
          └─ 不值得 → 不做
```

### 应用例子

**问：要不要加 LSP 支持？**
1. 解决哪个真理？→ 阅读代码的真理（更好的代码理解）
2. 是不是最直接的？→ 不是。直接的方式是 read_file + grep
3. 更好的方式？→ 让 AI 主动读取它需要的文件
4. 引入复杂性？→ 是的，LSP 要启动守护进程、管理状态
5. 值得吗？→ 当前 grep + read_file 组合能满足多数场景 → 不做

**问：要不要加 Docker 沙箱？**
1. 解决哪个真理？→ 执行代码的真理（安全隔离）
2. 是不是最直接的？→ 不是。直接的方式是不做隔离
3. 更好的方式？→ 等用户遇到安全问题再考虑
4. 引入复杂性？→ 很大
5. 值得吗？→ 当前不值得 → 不做

**问：要不要优化 system prompt 让回答更精炼？**
1. 解决哪个真理？→ 人机协作效率
2. 是不是最直接的？→ 是的。改几句话就行
3. 引入复杂性？→ 几乎没有
4. 值得吗？→ 显然值得 → 做

---

## 四、迭代准则

1. **从最简单的方案开始。** 不够好时，用户会告诉我们。
   `anvil compare` 的数据会量化"不够好"的程度。

2. **每次改动都应该能回答"它触达了哪个基本真理"。**
   如果答不出，这个改动可能是类比驱动的，不是第一性原理驱动的。

3. **代码量不是衡量标准。** 最优雅的代码是不存在的代码。
   删除一行代码比增加一行更有价值。

4. **配置统一在 `~/.anvil/`。**
   不读 `~/.zshrc`、不读 `~/.claude/`、不读环境变量（除了 tools 内部的运行时注入）。

5. **工具的演进方向由用户需求驱动，不由其他产品的功能列表驱动。**
   `anvil compare` 告诉我们"哪里需要改进"，而不是"哪个功能别人有我们没有"。
