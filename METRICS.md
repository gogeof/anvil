# ANVIL — 指标体系（Metrics）

> **指标驱动改进。** 不通过类比、不通过功能数量评价优劣。
> 只通过量化指标判断 anvil 是否在变好。

---

## 一、核心原则

### 1. 专注编程领域

anvil 只解决编程问题。不做通用聊天、不做知识问答、不做创意写作。

**验证方式：**
- System prompt 中不包含非编程相关指令
- 工具集只包含编程必需工具（读/写/执行/搜索）
- 对非编程任务的拒绝率应 > 80%

### 2. 第一性原理驱动

所有功能从四个基本真理推导：

```
编程 = 读 + 写 + 执行 + 搜索
```

**验证方式：**
- 每个工具必须有明确的"对应哪个基本能力"
- 新功能必须能回答"它解决了哪个基本真理"
- 不引入非必需的复杂性（LSP、向量数据库、Docker 沙箱等）

### 3. 指标驱动

没有度量就没有改进方向。所有优化必须：
1. 定义量化指标
2. 测量当前基线
3. 设定目标值
4. 追踪变化趋势

---

## 二、评估维度与指标

### 维度 1：搜索能力（Search）

编程中 40% 的时间在搜索代码、文档、方案。

| 指标 | 定义 | 当前基线 | 目标值 | 度量方法 |
|------|------|----------|--------|----------|
| **搜索延迟** | grep_search 返回结果的平均时间 | ~200ms (regex crate) | **< 50ms** (ripgrep) | 埋点统计 P50/P95 |
| **搜索准确率** | 返回结果中包含期望内容的比例 | 未测量 | **> 95%** | 测试集 + 人工标注 |
| **大项目支持** | 能在多少行代码的项目中正常工作 | 未测量 | **> 500K LOC** | 在 Linux kernel 等大仓库测试 |
| **搜索内存占用** | grep_search 时的峰值内存 | 未测量 | **< 100MB** | `/usr/bin/time -v` 统计 |

**优化方向（Phase 1）：**
- ✅ 集成 ripgrep crate（性能 5-10x 提升）
- ⏳ 添加符号索引（加速定义跳转）→ 已移除，使用 grep_search 直接实现
- ⏳ 支持增量搜索（边输入边搜索）

---

### 维度 2：阅读能力（Read）

理解现有代码是编程的基础。

| 指标 | 定义 | 当前基线 | 目标值 | 度量方法 |
|------|------|----------|--------|----------|
| **读取延迟** | read_file 返回内容的平均时间 | ~50ms | **< 30ms** | 埋点统计 |
| **大文件支持** | 能读取的最大文件大小 | 未测量 | **> 10MB** | 压力测试 |
| **语法感知** | 是否支持代码高亮、折叠 | ❌ 无 | **基础高亮** | 功能验证 |
| **上下文效率** | 读取的代码中有多少被 AI 使用 | 未测量 | **> 60%** | 追踪 AI 引用的代码片段 |

**优化方向（Phase 1）：**
- ⏳ 添加语法感知（识别函数/类/变量定义）→ 已移除，使用 grep_search 直接实现
- ⏳ 智能摘要（对大文件只读取关键部分）

---

### 维度 3：编写能力（Write）

编写新代码、修改现有代码。

| 指标 | 定义 | 当前基线 | 目标值 | 度量方法 |
|------|------|----------|--------|----------|
| **编辑成功率** | edit_file 一次成功的比例 | ~70% | **> 90%** | 测试集验证 |
| **模糊匹配率** | 能容错多少差异（空格、缩进） | ~10% | **> 50%** | 测试集验证 |
| **冲突处理** | 多次编辑同一文件的冲突率 | 未测量 | **< 5%** | 并发编辑测试 |
| **代码质量** | 生成的代码通过 lint/test 的比例 | 未测量 | **> 80%** | 执行测试验证 |

**优化方向（Phase 2）：**
- 🔄 增强模糊匹配（Levenshtein 距离、AST diff）
- ⏳ 添加代码格式化（rustfmt/prettier）
- ⏳ 集成 git diff（可视化变更）

---

### 维度 4：执行能力（Execute）

执行代码验证正确性。

| 指标 | 定义 | 当前基线 | 目标值 | 度量方法 |
|------|------|----------|--------|----------|
| **命令成功率** | bash 命令执行成功的比例 | 未测量 | **> 85%** | 埋点统计 |
| **后台执行** | 是否支持后台运行长时间命令 | ❌ 无 | ✅ 支持 | 功能验证 |
| **PTY 支持** | 是否支持交互式命令 | ❌ 无 | ✅ 支持 | 功能验证 |
| **超时处理** | 超时命令的优雅终止率 | 未测量 | **100%** | 压力测试 |

**优化方向（Phase 2）：**
- 🔄 后台执行模式（不阻塞 REPL）
- 🔄 PTY 支持（支持 vim/htop 等交互命令）
- ⏳ 进程管理（列出、监控、终止后台进程）

---

### 维度 5：整体效率（Overall）

端到端完成编程任务的能力。

| 指标 | 定义 | 当前基线 | 目标值 | 度量方法 |
|------|------|----------|--------|----------|
| **任务完成率** | 完成的编程任务占总任务的比例 | 未测量 | **> 80%** | HumanEval 等基准 |
| **任务完成时间** | 完成典型编程任务的平均时间 | 未测量 | **< 5 min** | 定时测量 |
| **交互轮次** | 完成任务需要的平均对话轮次 | 未测量 | **< 10 轮** | 埋点统计 |
| **错误恢复率** | 出错后能自动恢复的比例 | 未测量 | **> 70%** | 故障注入测试 |

**度量方法：**
- 使用 HumanEval、MBPP 等编程基准测试
- 自定义真实场景任务集（实现功能、修复 bug、重构代码）
- 对比 anvil 和参考基准工具的完成情况

---

### 维度 6：用户体验（UX）

影响开发者日常使用的感受。

| 指标 | 定义 | 当前基线 | 目标值 | 度量方法 |
|------|------|----------|--------|----------|
| **首字延迟** | 从输入到收到首个 token 的时间 | ~2s | **< 1s** | 埋点统计 |
| **响应流畅度** | Token 生成速率 | ~20 tokens/s | **> 30 tokens/s** | 埋点统计 |
| **上下文利用率** | 使用的上下文占总上下文的比例 | 未测量 | **> 50%** | 追踪 AI 请求 |
| **错误处理** | 错误信息对用户的帮助程度 | 未测量 | **> 4/5** | 用户调研 |

---

## 三、对标基准（Baseline）

### 对标方法

使用 `anvil compare` 命令与参考基准工具对比：

```bash
# 对比特定任务
anvil compare "实现一个线程安全的 LRU 缓存"

# 对比 HumanEval 任务
anvil compare --benchmark humaneval --task HumanEval/0

# 查看对比历史
anvil compare --list
```

### 参考基准工具

anvil 与以下工具对比（按可用性选择）：
1. **Claude Code CLI** — 当前最佳的 AI 编程助手
2. **Cursor** — 流行的 AI 编程 IDE
3. **GitHub Copilot CLI** — 官方 CLI 工具
4. **其他开源工具** — Aider、OpenHands 等

### 对比维度

每次对比记录：
- ✅ 任务是否完成
- ⏱️ 完成时间
- 📊 代码质量（lint/test 通过率）
- 🔄 交互轮次
- 📝 输出长度、代码块数量

---

## 四、优化路线图

### Phase 1：搜索与阅读（Week 1-3）

**目标：** 提升"读"和"搜索"能力到生产可用水平。

| 任务 | 指标改进 | 状态 |
|------|----------|------|
| 集成 ripgrep crate | 搜索延迟 < 50ms | ✅ 已完成 |
| ~~添加符号索引~~ | ~~定义跳转 < 100ms~~ | ❌ 已移除，使用 grep_search |
| ~~增强语法感知~~ | ~~高亮支持 5+ 语言~~ | ❌ 已移除，render.rs 已有实现 |

**成功标准：**
- 在 Linux kernel 仓库中搜索延迟 < 100ms
- 搜索准确率 > 90%

---

### Phase 2：编写与执行（Week 4-6）

**目标：** 提升"写"和"执行"能力，支持复杂场景。

| 任务 | 指标改进 | 状态 |
|------|----------|------|
| 增强模糊匹配 | 编辑成功率 > 90% | ✅ 已完成 |
| 后台执行支持 | 支持长时间命令 | ✅ 已完成 |
| PTY 支持 | 支持交互式命令 | ✅ 已完成 |

**成功标准：**
- edit_file 一次成功率 > 85%
- 支持后台运行编译、测试等长时间任务

---

### Phase 3：智能化（Week 7-8）

**目标：** 提升整体效率和用户体验。

| 任务 | 指标改进 | 状态 |
|------|----------|------|
| test_runner 工具 | 测试集成优化 | ✅ 已完成 |
| 调试辅助工具 | 错误恢复率 > 70% | ✅ 已完成 |
| 性能优化 | 首字延迟 < 1s | ✅ 已完成 |

**成功标准：**
- HumanEval 完成率 > 75%
- 典型任务完成时间 < 5 min

---

## 五、度量实施

### 1. 自动化测试集

创建 `tests/benchmarks/` 目录，包含：

```
tests/benchmarks/
├── search/           # 搜索能力测试
│   ├── large_repo.json
│   └── accuracy.json
├── read/             # 阅读能力测试
│   ├── large_file.json
│   └── syntax.json
├── write/            # 编写能力测试
│   ├── edit_accuracy.json
│   └── code_quality.json
├── execute/          # 执行能力测试
│   ├── long_running.json
│   └── interactive.json
└── humaneval/        # HumanEval 基准
    └── tasks.json
```

### 2. 埋点系统

在关键路径添加埋点：

```rust
// 示例：grep_search 埋点
let start = std::time::Instant::now();
let results = grep_search(pattern, path)?;
let latency = start.elapsed().as_millis();
telemetry::record("grep_search", json!({
    "latency_ms": latency,
    "pattern_len": pattern.len(),
    "results_count": results.len(),
}));
```

### 3. 定期报告

每周生成指标报告：

```bash
# 生成报告
anvil metrics report --week

# 输出示例
Week 20 (2026-05-08 ~ 2026-05-14)

Search:
  - Latency P50: 45ms ↓ (上周: 62ms)
  - Latency P95: 120ms ↓ (上周: 180ms)
  - Accuracy: 92% ↑ (上周: 88%)

Write:
  - Edit Success Rate: 78% → (目标: 90%)
  - Fuzzy Match Rate: 35% ↑ (上周: 20%)

Tasks:
  - HumanEval Pass Rate: 65% ↑ (上周: 58%)
```

---

## 六、不追求的指标

明确边界，避免偏离核心方向：

| 不追求 | 原因 |
|--------|------|
| **功能数量** | 工具不在多，在于覆盖核心需求 |
| **支持语言数** | 通过通用工具支持，不做语言特定优化 |
| **UI 美观度** | CLI 工具，专注于功能而非外观 |
| **集成第三方服务** | 保持供应商中性，避免锁定 |
| **通用聊天能力** | 专注编程，不做万能助手 |

---

## 七、总结

anvil 的优劣只通过以下指标判断：

```
搜索延迟 < 50ms      → 快
编辑成功率 > 90%     → 准
任务完成率 > 80%     → 强
首字延迟 < 1s        → 爽
```

其他一切（功能数量、UI 美观、类比对象）都是噪音。

**持续改进，数据说话。**
